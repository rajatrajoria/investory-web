# Copy this file to .env.local for development, and configure the same
# values as environment variables in the cPanel "Setup Node.js App" panel
# for production. Never commit .env.local or any file with real secrets.

# --- Database (MySQL on GlobalHost) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=investor_wp341
DB_PASSWORD=change-me
DB_NAME=investor_wp341

# --- Sessions / CSRF ---
# Generate with: openssl rand -base64 32
SESSION_SECRET=change-me-to-a-random-32-plus-character-string

# --- Outbound email (contact form notifications only) ---
SMTP_HOST=mail.investory.co.in
SMTP_PORT=465
SMTP_USER=ramankhandelwal@investory.co.in
SMTP_PASSWORD=change-me
SMTP_FROM=ramankhandelwal@investory.co.in
CONTACT_TO_EMAIL=ramankhandelwal@investory.co.in

# --- Site ---
SITE_URL=https://www.investory.co.in

# --- Optional bot protection on the contact form (leave blank to disable) ---
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
